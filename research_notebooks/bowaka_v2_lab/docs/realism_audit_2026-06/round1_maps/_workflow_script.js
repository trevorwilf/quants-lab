export const meta = {
  name: 'bowaka-v2-realism-audit-map',
  description: 'Round 1: map lab subsystems, extract prod strategy contract, inventory tests/audits, research Alpaca data catalog',
  phases: [
    { title: 'Lab', detail: 'deep-read every lab subsystem' },
    { title: 'Prod', detail: 'extract live strategy behavioral contract from mirror' },
    { title: 'Tests', detail: 'inventory test suite' },
    { title: 'History', detail: 'prior audits + known-issue registry' },
    { title: 'Alpaca', detail: 'web research: Alpaca data products' },
  ],
}

const COMMON = `READ-ONLY DISCOVERY. Do not create, modify, or delete ANY file. You are auditing a quant trading simulator for realism.
Work ONLY inside E:\\tradingsoftware\\quants-lab. LAB = E:\\tradingsoftware\\quants-lab\\research_notebooks\\bowaka_v2_lab. COMMON = E:\\tradingsoftware\\quants-lab\\research_notebooks\\bowaka_common.
The live production strategy is mirrored at LAB\\reference\\source_strategy\\scripts\\ — read the mirror only; NEVER read E:\\stocktradingsoftware.
Platform is Windows; use the Read/Grep/Glob tools with absolute Windows paths.
Your final message is consumed by an orchestrator, not a human. Return a dense markdown report (<= 1700 words) with these sections:
## Purpose & data flow (how this subsystem fits the backtest pipeline)
## Behavioral spec (precise bullets; EVERY nontrivial behavior with exact file:line refs)
## Knobs (config fields read here: name, default, what happens when on/off, where threaded)
## Invariants & guards (assertions, fail-loud paths, silent-fallback paths — flag every silent fallback explicitly)
## Leads (suspected bugs, realism gaps, dead code, TODO/FIXME, anything smelling wrong — file:line + one-line rationale each; do NOT deep-verify, just flag)
## Test coverage hooks (which test files exercise this; note any behavior with NO test)
Be exhaustive on Leads — false positives are acceptable, they get adversarially verified later. Prioritize accuracy over speed: actually read the files end to end, do not skim.`

const LAB_SPECS = [
  { key: 'sim_core', label: 'lab:sim-core', files: 'LAB\\src\\bowaka_v2_lab\\sim\\: backtester.py (2083 ln — read ALL of it), event_loop.py, schedule.py, exit_driver.py, risk_gates.py, protection.py, portfolio.py. Also glob the sim/ dir first and skim any file I did not list.' ,
    extra: 'Focus: bar/event sequencing, intrabar ordering assumptions, session boundaries (open/close/half-days?), how positions/PnL/equity evolve, multi-day holds, risk kill behavior, what the manifest records, run modes (smoke_fixture/current_code_parity/fast_realism/intended_realism) and exactly what each mode changes.' },
  { key: 'sim_fills', label: 'lab:fills-tape-quotes', files: 'LAB\\src\\bowaka_v2_lab\\sim\\: fills.py (1254 ln — read ALL), tape_fill.py, quote_model.py. Glob sim/ for related fill/tape files.',
    extra: 'Focus: the full fill model — tier ladder (T0..T3), FOK semantics, participation/depth assumptions, slippage, spread handling, quote-age gating, the tape_replay fill path (entry + how exits call it), partial fills, where fills can exceed displayed/traded liquidity, latency modeling (or absence). Document the EXACT price formation formula per tier/mode.' },
  { key: 'sim_exits', label: 'lab:exits-consumer', files: 'LAB\\src\\bowaka_v2_lab\\sim\\: exits.py (1499 ln — read ALL), strategy_consumer.py (843 ln — read ALL).',
    extra: 'Focus: OCO bracket semantics (stop vs target intrabar ordering, gap handling, same-bar both-touched resolution), signal-fade exits, time exits, end-of-day handling, the numpy vs pandas exit walk duality, tape_replay exit branch + target-side clamp, sizing (equal_slice, compounding overlay, floor halt), order construction, cooldowns, re-entry rules.' },
  { key: 'scanner', label: 'lab:scanner-features', files: 'LAB\\src\\bowaka_v2_lab\\scanner\\: scan_loop.py, scan_matrix.py (1486 ln), scan_matrix_vectorized.py, scan_matrix_runtime.py, session_minute_window_cache.py. LAB\\src\\bowaka_v2_lab\\features\\: forming_bar.py, volume_curve.py, _numba_scan_features.py and any others.',
    extra: 'Focus: how candidates are screened each minute, forming-bar semantics (does the scanner see the in-progress minute bar? PIT correctness), rvol/range-expansion/ema-slope computations, the precomputed scan-matrix path vs live scan_loop path and their claimed equivalence, volume curve construction, lookback windows, timezone handling.' },
  { key: 'data', label: 'lab:data-lake', files: 'LAB\\src\\bowaka_v2_lab\\data\\: suppliers.py, cached_suppliers.py, data_quality.py (1631 ln — read ALL), dq_levels.py, lineage.py, loaders.py, verify_lake.py, daily_cache_batch.py. COMMON\\src\\bowaka_common\\marketdata\\: store.py, layout.py, backfill.py (skim backfill, read store/layout fully). Also read LAB\\docs\\data_lake_layout.md and E:\\tradingsoftware\\quants-lab\\research_notebooks\\market_data\\README.md if present.',
    extra: 'Focus: lake schema (bars 1m/1d, adjustment partitions, trades, quotes_fine), exactly what each supplier returns (columns, tz, units), carry-forward/forward-fill behavior, DQ gates (min-bar density, PIT eligibility) and which gates can silently drop symbols/sessions, split/dividend adjustment handling (minute RAW vs daily split_adjusted), quote sampling (prevailing NBBO per minute — bias?), trades tape schema, dataset hashing/lineage.' },
  { key: 'optuna_core', label: 'lab:optuna-core', files: 'LAB\\src\\bowaka_v2_lab\\optuna\\: walkforward_runner.py (3206 ln — read ALL of it, it is the heart), fold_context.py, objective.py, search_space.py.',
    extra: 'Focus: walk-forward fold construction (train/validate windows, embargo/purge?), per-fold universe building, the objective formula (penalties, collapse handling, why scores can be negative with positive PnL), incumbent trial-0, search space bounds vs live values, pruning, caching layers (startup DQ cache, dq_cache_symbol_set, signal/matrix caches) and what could leak across folds/trials.' },
  { key: 'optuna_validation', label: 'lab:validation-gates', files: 'LAB\\src\\bowaka_v2_lab\\optuna\\: preflight.py, holdout.py, robustness.py, promotion_gates.py, evaluate_finalists.py, stress_matrix.py, study_validity.py, pit_universe.py, autoconfig.py, parallel.py, dispatcher.py, stop_ship_checklist.py. LAB\\src\\bowaka_v2_lab\\promotion\\: checklist.py, suitability.py.',
    extra: 'Focus: what gates a config must pass before promotion, holdout isolation (can holdout data leak into search?), stress matrix dimensions and whether they actually perturb anything at defaults, finalist evaluation (derive_validation_config, enable_tape_replay), suitability tier ladder and what caps at research_only/backtesting_only, preflight checks, multiprocess dispatch determinism.' },
  { key: 'config_universe', label: 'lab:config-universe-reference', files: 'LAB\\src\\bowaka_v2_lab\\universe\\builder.py, LAB\\src\\bowaka_v2_lab\\config\\: models.py, loader.py, parity_sidecar.py, config_diff.py. LAB\\src\\bowaka_v2_lab\\reference\\: __init__.py, import_config.py. Also read the first 120 lines of LAB\\reference\\actual_bowaka_v2_contract.yaml and ALL of LAB\\configs\\bowaka_v2_actual_sip_intended_realism.yml and LAB\\configs\\bowaka_v2_intended_realism.yml.',
    extra: 'Focus: PIT universe construction (ADV/price/share filters, prior-day baselines, the no-prior-daily-bar guard), the contract->lab-config mapping (which live fields map, which are accept-and-ignore, which are MISSING), config schema defaults that differ from prod values, the parity sidecar mechanism.' },
  { key: 'reconcile_reports', label: 'lab:reconcile-parity-reports', files: 'LAB\\src\\bowaka_v2_lab\\reconcile\\: comparators.py, report.py, replay.py, importer.py, slippage_residuals.py, oco_latency_calibrator.py, schemas.py, paper_log_schema.py, comparator.py, orchestrator.py. LAB\\src\\bowaka_v2_lab\\parity\\: runner.py (1049 ln), metrics.py, normalizers.py, golden.py, report.py. LAB\\src\\bowaka_v2_lab\\reports\\: render_run_report.py, exit_analysis.py, execution_quality.py. LAB\\src\\bowaka_v2_lab\\schemas\\: events.py, decisions.py.',
    extra: 'Focus: paper-trading reconciliation (what live-vs-sim divergences it can detect, what it cannot), the lab-vs-prod parity runner (what it actually compares — configs? signals? fills? PnL?), slippage residual calibration, OCO latency calibration, what the run report surfaces vs hides.' },
]

const PROD_SPECS = [
  { key: 'prod_strategy', label: 'prod:strategy', files: 'LAB\\reference\\source_strategy\\scripts\\: bowaka_v2_strategy.py (2039 ln — read EVERY line), bowaka_v2_config.yaml (all 334 ln), bowaka_v2_schemas.py, _retighten_oco_at_market_open.py.',
    extra: 'This is the LIVE production strategy the simulator must replicate. Extract its complete behavioral contract: main loop cadence/clock, entry signal pipeline (score, thresholds), order placement (order types! FOK? limit? bracket/OCO? extended hours?), exit management (stop/target/signal-fade/time/eod), the retighten-OCO-at-open job, sizing incl. compounding (_sizing_bankroll/_below_floor), risk gates (daily loss, halt handling?, max positions), quote-age/spread gating, error/retry paths, state persistence, anything time-of-day dependent. List EVERY config key in bowaka_v2_config.yaml with its live value (this is the parity anchor). Note behaviors that would be HARD to simulate from bar/tape data (e.g. websocket race conditions, broker rejects).' },
  { key: 'prod_scanner', label: 'prod:scanner-universe', files: 'LAB\\reference\\source_strategy\\scripts\\: bowaka_intraday_scanner.py (921 ln — read ALL), bowaka_v2_features.py (602 ln — read ALL), bowaka_v2_volume_curve.py, bowaka_universe_builder.py (771 ln — read ALL), bowaka_universe_builder.yaml.',
    extra: 'Extract: how the live scanner builds its candidate list each cycle (data source, cadence, forming-bar usage), every feature formula (rvol_so_far, projected_full_day_rvol, range_expansion, ema_slope, score weights) with exact math, the universe builder nightly job (filters: price/ADV/exchange/asset-class, data fetched, schedule), volume curve construction. Note any live-side behavior dependent on data the lab lake might lack.' },
  { key: 'prod_execution', label: 'prod:execution-data', files: 'LAB\\reference\\source_strategy\\scripts\\: bowaka_v2_openalgo_client.py (445 ln — read ALL), bowaka_v2_stream.py, bowaka_v2_alpaca_data.py, bowaka_v2_cost_model.py, bowaka_v2_backtest.py (568 ln), bowaka_v2_replay.py, bowaka_v2_replay_data.py, bowaka_v2_heartbeat.py, bowaka_v2_paths.py, bowaka_v2_event_stream_versioning.py. Skim bowaka_v2_dashboard.py, bowaka_v2_analysis.py, bowaka_v2_counterfactuals.py, bowaka_v2_ablation.py, bowaka_v2_bucket_analysis.py, _validate_alpaca_parity.py.',
    extra: 'Extract: the broker order API semantics (openalgo client: order types, time-in-force, what a reject/partial looks like, polling vs callbacks, latencies), live market-data acquisition (stream vs REST, which Alpaca feed, quote/trade subscriptions, staleness handling), the prod cost model (fees/slippage assumptions), what the prod backtester itself simulates (it is a SECOND simulator — note where its assumptions differ from the lab), replay tooling.' },
]

phase('Lab')
const results = {}
const labP = LAB_SPECS.map(s => () =>
  agent(`${COMMON}\n\nSUBSYSTEM: ${s.key}\nFILES TO READ: ${s.files}\n${s.extra}`, { label: s.label, phase: 'Lab' })
    .then(r => { results[s.key] = r; log(`done: ${s.key}`); return r }))
const prodP = PROD_SPECS.map(s => () =>
  agent(`${COMMON}\n\nSUBSYSTEM: ${s.key}\nFILES TO READ: ${s.files}\n${s.extra}`, { label: s.label, phase: 'Prod' })
    .then(r => { results[s.key] = r; log(`done: ${s.key}`); return r }))

const testsP = [() => agent(`${COMMON}\n\nSUBSYSTEM: test-suite inventory.\nThere are 538 test files: tests/unit (151 + subdirs), tests/integration (175), tests/parity (55), tests/reconcile (8), tests/scanner (1). Read LAB\\tests\\conftest.py fully, glob the whole tests tree, then READ a representative 25-30 test files spanning all categories (pick the ones guarding fills, exits, parity goldens, walkforward, data quality, tape replay, compounding, scan matrix). Also glob LAB\\tests\\fixtures\\ and read 3-4 fixture builders (e.g. build_sip_synthetic_lake.py, iex_short_run_lake.py).\nFocus: (a) WHAT each category actually asserts (behavior vs golden-pinning vs smoke), (b) which tests run on SYNTHETIC data vs the REAL lake/tape (the operator explicitly distrusts synthetic-data validation — classify!), (c) xfail/skip/env-gating patterns that could mask failures on this machine, (d) fixtures that bypass the production code path, (e) obviously tautological tests (asserting the implementation against itself), (f) major UNTESTED surfaces. Replace the 'Knobs' section with '## Synthetic vs real-data census' (estimated counts per category).`, { label: 'tests:inventory', phase: 'Tests' })
    .then(r => { results.tests = r; log('done: tests'); return r })]

const historyP = [() => agent(`${COMMON}\n\nSUBSYSTEM: prior-audit history + known-issues registry.\nRead ALL of: LAB\\docs\\audits\\2026-05-22_realism_audit.md, 2026-05-23_realism_audit.md, 2026-05-29_bowaka_v2_lab_audit_report_revised.md, 2026-06-07_intended_realism_coverage_findings.md; LAB\\docs\\fill_realism.md; LAB\\docs\\current_code_vs_intended_realism.md; LAB\\docs\\alpaca_fill_realism_implementation_plan.md; LAB\\docs\\sip_migration_runbook.md; glob LAB\\PHASE_NOTES\\ and read anything fill/realism related; E:\\tradingsoftware\\quants-lab\\research_notebooks\\bowaka_v2_lab\\README.md if present.\nProduce instead of the standard sections: ## Timeline of audits & remediations (what each found, what shipped); ## CLOSED items (resolved findings — so the new audit does not re-litigate them); ## OPEN/DEFERRED items (explicitly deferred or known-infeasible, with the stated reason); ## Claimed-but-worth-rechecking (items marked fixed where the fix was partial, default-off, or waived); ## Realism mode matrix (what current_code_parity vs fast_realism vs intended_realism each enable per these docs). Exact doc + section refs throughout.`, { label: 'history:audits', phase: 'History' })
    .then(r => { results.history = r; log('done: history'); return r })]

const ALPACA_COMMON = `You are researching Alpaca Markets (alpaca.markets) Market Data API capabilities as of mid-2026 for a US-equities intraday strategy backtester. Use WebSearch + WebFetch on docs.alpaca.markets (Market Data API v2 docs) and the Alpaca changelog/blog. NO file system work needed. Return dense markdown (<= 1700 words) with exact endpoint names, field lists, and doc URLs. Distinguish clearly: available on the SIP feed historical API vs IEX feed vs streaming-only vs Broker-API-only vs not available at all.`

const alpacaP = [
  () => agent(`${ALPACA_COMMON}\nTOPIC 1 — full data-product catalog for stocks: historical bars (timeframes, adjustment options raw/split/dividend/all, feed param), historical trades (full field list incl. condition codes, exchange codes, tape, update/correction handling), historical quotes (NBBO field list, conditions), AUCTIONS endpoint (opening/closing auction prices — fields, coverage), snapshots, exchange & condition-code metadata endpoints, corporate actions API (types: splits, dividends, mergers, symbol changes...), news API, options data, crypto (skip detail), screener/most-actives, websocket streams (trades/quotes/bars/updatedBars/dailyBars/statuses/lulds/corrections/cancelErrors — enumerate ALL stream channels), historical data coverage start dates, rate limits per subscription tier, and pagination/latency characteristics of historical endpoints.`, { label: 'alpaca:catalog', phase: 'Alpaca' })
    .then(r => { results.alpaca_catalog = r; log('done: alpaca catalog'); return r }),
  () => agent(`${ALPACA_COMMON}\nTOPIC 2 — microstructure realism specifics a backtester needs: (a) trading HALTS & LULD: does Alpaca expose halt status or LULD bands historically? (the 'statuses' and 'lulds' websocket channels — is there ANY historical/REST equivalent? can halts be reconstructed from trade/quote condition codes — which codes?); (b) trade condition codes: which codes mark opening/closing auction prints, odd lots, late/out-of-sequence, average-price, derivatively-priced — i.e. which prints should a fill simulator EXCLUDE from a tape-replay fill model, and which flags affect official volume/high/low; (c) quote conditions incl. slow/crossed/locked markets, and whether NBBO quote sizes are round lots (size in lots vs shares) — odd-lot quotes visible?; (d) auction data: how to get official open/close prices (auction endpoint vs 'o'/'c' condition prints vs daily bars); (e) extended-hours data availability in bars/trades/quotes; (f) corporate-actions timing (when published, point-in-time safe?); (g) short-sale data: SSR flag availability? borrow/HTB data?; (h) bar construction rules (which trade conditions are included in OHLCV minute bars — Alpaca's documented bar-aggregation rules). Cite doc URLs.`, { label: 'alpaca:microstructure', phase: 'Alpaca' })
    .then(r => { results.alpaca_micro = r; log('done: alpaca microstructure'); return r }),
]

// Run in waves of 5 to avoid the server-side rate limiting that killed the
// first full-burst attempt (15/16 agents throttled). Cached calls return instantly.
const thunks = [...labP, ...prodP, ...testsP, ...historyP, ...alpacaP]
for (let i = 0; i < thunks.length; i += 5) {
  log(`wave ${Math.floor(i / 5) + 1} of ${Math.ceil(thunks.length / 5)}`)
  await parallel(thunks.slice(i, i + 5))
}
return results